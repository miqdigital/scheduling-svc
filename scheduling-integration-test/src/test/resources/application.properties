schedulingServiceEnv=local
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.quartz-data-source.jdbc-url=${DB_URL}
spring.datasource.quartz-data-source.username=${DB_USERNAME}
spring.datasource.quartz-data-source.password=${DB_PASSWORD}