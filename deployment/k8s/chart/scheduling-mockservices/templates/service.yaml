apiVersion: v1
kind: Service
metadata:
  name: {{ include "scheduling-mockservices.fullname" . }}
  labels:
{{ include "scheduling-mockservices.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "scheduling-mockservices.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
