apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scheduling-mockservices.fullname" . }}-test-connection"
  labels:
{{ include "scheduling-mockservices.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "scheduling-mockservices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
